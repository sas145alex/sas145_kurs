%tr.nested-fields
  %td
    = link_to_remove_association (fa_icon :remove), f, class: 'btn btn-danger'
  %td
    = f.number_field :rack_number, class: 'form-control'
  %td
    = f.collection_select :book_id, Book.all, :id, :name
