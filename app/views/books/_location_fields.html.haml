%tr.nested-fields
  %td
    = link_to_remove_association (fa_icon :remove), f, class: 'btn btn-danger'
  %td
    = f.text_field :book_id
  %td
    = f.collection_select :shelf_id, Shelf.all, :id, :shelf_index
    -# = f.select :shelf_id, options_for_select(Shelf.all.map{|s| [s.shelf_index,s.id]})
  -# %td
  -# = f.hidden_field :book_id
  %td
    = f.number_field :rack_number, class: 'form-control'
